# Supabase project — Valiz Bitácora
# Public values: safe to commit this example file. Real values live in .env.local (gitignored).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solo para el script local sync_valiz.py — NO subir nunca, NO usar en frontend.
# Bypassa RLS; con este key se puede leer y modificar TODO en la DB.
SUPABASE_SERVICE_ROLE_KEY=
